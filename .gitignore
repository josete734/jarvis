# secrets
.env
*.restic-pass
config/spotify_refresh.txt        # refresh token de Spotify (NUNCA al repo público)
*refresh*.txt
*secret*
*.npy                             # embeddings de cara (biometría)

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# artifacts
*.wav
speech_test/
.DS_Store

# local data lives OUTSIDE the repo (/srv/jarvis, /var/lib/jarvis/models)
data/

# build de la tesis (artefactos pandoc)
docs/tesis/build/
docs/tesis/_check.txt
docs/tesis/*.pdf

# build de la tesis2 (artefactos pandoc)
docs/tesis2/build/
