[Unit]
Description=J.A.R.V.I.S. backup diario (restic a /mnt/backup)
Wants=network-online.target
After=network-online.target docker.service

[Service]
Type=oneshot
User=root
ExecStart=/opt/jarvis/scripts/backup.sh
# backup.sh aborta limpio si /mnt/backup no está montado (no escribe nada).
