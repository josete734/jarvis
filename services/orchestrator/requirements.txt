# Pinned per PLAN_FINAL §2. Extras verificados contra el pyproject de pipecat-ai v1.3.0:
#   local→pyaudio · whisper→faster-whisper · piper→piper-tts · mem0→mem0ai · openai→SDK base.
#   'silero' es un extra VACÍO (el VAD corre sobre el onnxruntime de la base); openwakeword va aparte.
pipecat-ai[local,whisper,piper,mem0,openai,elevenlabs]==1.3.0
openwakeword==0.6.0   # no es extra de pipecat; necesita onnxruntime (heredado de pipecat base)
chromadb==1.5.9       # mem0 1.x NO lo arrastra; cliente==server (un cliente <0.5.23 hablaría /api/v1 y el server 1.5.9 da 410)
trafilatura==2.1.0
aiohttp>=3.9,<4
pyyaml>=6
loguru
livekit            # AEC plan C (PLAN_FINAL §5.2) - activado sesión 4
pytest>=8          # solo tests: `docker compose exec orchestrator python3 -m pytest tests/`
# sentence-transformers>=3,<4  # (mem0/e5, Fase 3 aparcada — re-añadir al activar MEM0; arrastra torch ~2GB)
