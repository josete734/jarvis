<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema base de plataforma; el color real lo aplica Compose Material3 en runtime -->
    <style name="Theme.JarvisGlasses" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

</resources>
